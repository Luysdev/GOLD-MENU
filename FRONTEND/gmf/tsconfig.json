{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "esModuleInterop": true,  // Adicione essa linha para corrigir o erro
    "paths": {
      "@/*": [
        "./*"
      ]
    },
    "jsx": "react-native"  // Já está correto para JSX no React Native
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts"
  ]
}
